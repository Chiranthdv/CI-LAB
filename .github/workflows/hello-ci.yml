# name: Greeting Workflow
# on: [push]

# jobs:
#   say-hello:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Run a greeting
#         run: echo "The CI pipeline has successfully started!"

name: Greeting Workflow
on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4   # Step 1: download your code
      - name: Run Python Tests       # Step 2: run tests
        run: python3 test_calc.py
