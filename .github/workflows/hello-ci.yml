# name: Greeting Workflow
# on: [push]

# jobs:
#   say-hello:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Run a greeting
#         run: echo "The CI pipeline has successfully started!"

# name: Greeting Workflow
# on: [push]

# jobs:
#   say-hello:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install pytest
#         run: pip install pytest
#       - name: Run Python Tests
#         run: pytest -v
name: Weather App Production Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install Dependencies
        run: npm install

      # Unit tests run independently (no secrets needed)
      - name: Run Unit Tests
        run: node test.js

      # Security check with API key pasted directly here
      - name: Run Security Check
        run: node app.js
        env:
          WEATHER_API_KEY: "your-actual-api-key-here"

  package:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Create Build Artifact
        run: zip -r weather-app-dist.zip . -x "*.git*"
        
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: weather-app-dist
          path: weather-app-dist.zip
